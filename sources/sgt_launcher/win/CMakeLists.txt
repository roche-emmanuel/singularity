SET(TARGET_NAME "sgtLauncher")

FILE(GLOB_RECURSE PUBLIC_HEADERS "../include/*.h")
FILE(GLOB_RECURSE SOURCE_FILES "../src/launcher.cpp")

INCLUDE_DIRECTORIES(../../sgtLuna/include)
INCLUDE_DIRECTORIES(${WX_INC_DIR})

ADD_RESSOURCE(../src/launcher.rc SOURCE_FILES)

# windows app:
ADD_DEFINITIONS(-DSGT_WINDOWS)
ADD_EXECUTABLE (${TARGET_NAME} WIN32 ${SOURCE_FILES})

TARGET_LINK_LIBRARIES(${TARGET_NAME} sgtLuna ${BOOST_LIBS} ${LUA_LIBS} ${OSG_LIBS} ${FLAVOR_LIBS} ${GL_LIBS})

INSTALL_PDB()

INSTALL(TARGETS ${TARGET_NAME} DESTINATION bin/${BIN_FOLDER}/)

COMPRESS_BINARY()

