- In progress:
  * Retrieve fovX angle in osg/land/TerrainNode.lua, line 53.

- Libraries:

* SFML : http://www.sfml-dev.org/index-fr.php
* ngPlant


- Todo:

TODO:
    * Implement ProcedureCall class... 
	* Implement ComboTree:inferTypeTree()
    * Implement ComboTree:infer_vertex_type(tr, lit, arg_types) line 46 in ComboTree.lua
    * Implement Vertex:getArity()  

	* Implement complete Wescam overlays.
	
* build glew plugin.
* Check JSBSim for flight dynamics model: http://jsbsim.sourceforge.net/
* Check flightgear too.

* Add support for delay callback.

* Study crash with "Insufficient tile cache size for" in core\proland\terrain\TileSampler.cpp line 443 

* install opengl debug callback: example from ork/ui/GlutWindow.cpp

* Setup unhandled exception handler.

* backbone.js: http://backbonejs.org/
* http://www.chartjs.org/

* CityGML integration.

* CHeck proland sources and website: http://proland.inrialpes.fr/ and svn svn://scm.gforge.inria.fr/svnroot/proland/trunk

* look for: "Real-Time Procedural Planet Rendering"

* ubidisplays: https://code.google.com/p/ubidisplays/

* http://jrmtech.com/Products/SigSimRT

* FindFirstChangeNotification from http://msdn.microsoft.com/en-us/library/aa364417%28VS.85%29.aspx

* investigate comboreduct implementation in lua.

* try to build for linux32/linux64

* create a C++ storage to share common values in multi lua states.
* provide vector iterator support.
* create double entry map.

* Add integral types pointer support
* Subtexture upload for awesomium surface.

* Add minimal notebook app.
* Implement all listeners for webview
* Remove "<" as a ignoring function pattern (in wx module at least) ?

* Add support for array public variable access (like float _values[3];)
* return arguments passed by reference if they are not pointed classes.
* Create a more versatile loader from AssImp as lua module.


* Implement LUA IDE with module parsing support and auto completion/Function help.
* improve subclass registration to add class access in parent class (like osg.Drawable.CullCallback = osg.Drawable_CullCallback)
* add type auto-registration in wrapper writer when pushing args and results.
* add support for int variables in namespaces (like const in AK_KEY_1 = 0x89;)
* Find how to remove circular dependency in wrapper/table creation process.
* integrate AuiManager usage in ControlInterface ?: 

    wxTextCtrl* text1 = new wxTextCtrl(this, -1);
    wxTextCtrl* text2 = new wxTextCtrl(this, -1);
    m_mgr.AddPane(text1, wxLEFT, "Pane Caption");
    m_mgr.AddPane(text2, wxBOTTOM, "Pane Caption");
    m_mgr.Update();

	
* Upgrade version of doxmlparser

* Implement financial market predictor with:
  - neural network
  - colline society
  - opencog system from: https://github.com/opencog/opencog
  
